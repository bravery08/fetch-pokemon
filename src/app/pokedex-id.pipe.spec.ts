import { PokedexIdPipe } from './pokedex-id.pipe';

describe('PokedexIdPipe', () => {
  it('create an instance', () => {
    const pipe = new PokedexIdPipe();
    expect(pipe).toBeTruthy();
  });
});
